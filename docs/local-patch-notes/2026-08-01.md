# 2026-08-01

- Added the "projected sessions" briefing compiler (v1 Phase 1, then a same-day v2 rewrite into a fully self-contained module) — periodically compiles a digest/briefing from recent activity for delivery to Lumen.
- Added `host-cron`, a way to schedule host-shim scripts directly on a cron without spawning an agent container — cheaper than a full task wake for scripts that don't need agent reasoning.
- Added the MBIF vault memory pipeline: live per-turn transcript export into the vault, plus daily and weekly/monthly digest/rollup jobs (`digest-daily-host`, `digest-rollup-host`) driven by a "digester"/"librarian" agent.
- Added `add-host-scripts`, giving host-shim scripts per-agent-group segregation instead of being shared/global.
- Agent groups can now override container `env` vars and `blockedHosts` per group, rather than only at the global container-config level.
- Corresponding install skills (`add-mbif-vault`, `add-vault-memory-pipeline`, `add-projected-sessions`, `add-host-cron`) and the `recipe` applied-customizations ledger were documented.
