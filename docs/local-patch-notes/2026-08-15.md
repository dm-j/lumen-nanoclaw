# 2026-08-15

- Session-sync Phase 2: built out the remaining machinery — host-to-container and container-to-host push paths, per-session spawn credentials, reconnect-with-backoff, drain-before-exit on shutdown, and the inbound-DB mount-flip/write-path rewrite that switches a session over to the new transport. Still not live on any real group; a written switchover plan exists for turning it on reversibly.
- Fixed projected sessions silently accumulating unbounded context over time.
- Folded a set of briefing-host and transcript-append-host bugfixes (found on the live install, including a missed image-attachment feature on a stale seeded copy) back into the shared trunk template so future groups get them by default.
- Added a `focus-updater` step to Lumen's briefing-host: splits her rolling working memory into three independently-updated band files (Now/Soon/Back Burner), surfaced to her directly rather than only to Briefer.
