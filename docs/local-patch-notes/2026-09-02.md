# 2026-09-02

- Fixed scheduled tasks so a cold wake that's purely a gated task run skips the pre-spawn briefing compile step (avoids unnecessary work/latency); documented that a task's `--script` gate check runs inside the container, not on the host.
