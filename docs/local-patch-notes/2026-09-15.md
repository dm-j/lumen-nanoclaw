# 2026-09-15

- Agent groups now carry a description field, and an auto-generated routing table of available agent groups is composed into a coordinator agent's prompt — the foundation for multi-agent delegation (a "Dispatcher" style coordinator routing to specialist agents).
- Added a `research` container skill and documentation on PrefixRouter cache status / model-routing.
