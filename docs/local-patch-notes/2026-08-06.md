# 2026-08-06

- New subagents created via `create-agent` now inherit their parent's `env`/`blockedHosts` container overrides instead of starting blank.
- Vault transcript live export is now gated behind a per-agent-group opt-in flag rather than always-on.
- Projected-sessions briefing formatting improved further: batch entries render as markdown blockquotes with a timestamp+sender prefix.
