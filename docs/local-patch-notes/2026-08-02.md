# 2026-08-02

- Added the Telegram channel adapter.
- Fixed a bug where projected-session wakes could miss the transcript/briefing update on an already-warm container; added `remember`/`recall` MCP tools for agents to write and query vault memory directly.
