# 2026-08-14

- Session-sync Phase 1 landed: a host-side WebSocket server plus a generic container-side mirror client, with cert pinning, token refresh before expiry, and full-schema coverage on the outbound-DB side. Not yet turned on for any real agent group.
- Added tooling to keep the local Bun version used for agent-runner checks in sync automatically.
