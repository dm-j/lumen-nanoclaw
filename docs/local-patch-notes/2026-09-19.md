# 2026-09-19

- Ran a full roadmap-staleness sweep (one research agent per open item, checking it against actual codebase state): closed 3 items that had already been resolved as a side effect of unrelated work (Dispatcher's missing-specialist gap closed by building Routine; a task-handling scope ambiguity resolved by Routine's own ncl-tasks-vs-task_management clarification; a wikilink-query item superseded by the `read_wikilink` tool), and corrected stale wording on 4 more (ICS handling, email handling, the travel-time shim writeup, and session-sync-transport — the last gaining a "current status" note that the mechanism is fully built but still not live anywhere, with no further investigation of the Docker networking blocker since it was shelved). Also promoted the new routine-vault-calendar item to next-up in the roadmap at David's request. Following the sweep, tightened the roadmap-maintenance rule itself to explicitly call for scanning the rest of the index after finishing unrelated work, not just reacting when asked.
- Bumped `tsx` to 4.23.13 to drop a Node deprecation warning (`DEP0205`) that every `ncl`/`pnpm exec tsx` invocation was printing to stderr; the existing `^4.19.0` range already allowed it, it just hadn't been pulled. Prompted by this, added a "Minimal Friction" principle to CLAUDE.md: don't silently route around a surfaced problem out of habit, since doing so is a standing cognitive tax and denies the chance to direct a real fix at it.
