# 2026-08-20

- Cleaned up dead projected-sessions code left over from an earlier merge, and fixed a forward-compatibility gap for sessions predating the session-sync state table, plus briefing prepended-section stripping and recall-tool wording.
- The `focus-updater` working-memory band system (added a few days earlier) was consolidated into a single call covering all three bands at once, for cost and so an item could move between bands — but live-testing found it unsafely rewriting unrelated existing items on turns that never mentioned them. Deactivated pending a redesign; a separate `FOCUS_MODEL` knob was added so the feature's model choice can be tuned independently of the main briefing model once it's back on.
- Added `lumen-voice`, a standalone prose-to-script annotation tool (extracted from an external audiobook project) for turning text into per-line TTS voice-direction scripts.
