# 2026-08-05

- Hardened the host-shim/mcp-shim/projected-sessions stack: retry transient `SQLITE_CORRUPT` on inbound polling, self-heal messages stuck in "staged" status, stop persisting no-op briefings, include due reminders in the briefer's batch, and skip briefer dispatch entirely on empty batches.
- Moved mcp-shim/host-shim implementation scripts off the agent's mounted workspace (so agents can't accidentally edit their own tool implementations) and started tracking mcp-shims scripts under `groups/` as git-tracked capability code.
- Improved briefing readability: interleaved history, real sender names, local timestamps, and overwrite-logged the briefer's prompt/response for debugging.
- Documented the mcp-shims calling contract (PATH behavior, JSON-envelope wrinkles, per-script timeout override) and backfilled missing `recipe` ledger entries.
