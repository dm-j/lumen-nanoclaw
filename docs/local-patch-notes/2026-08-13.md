# 2026-08-13

- Started the session-sync project: a WebSocket-based replacement for the bind-mounted session DBs (Phase 0 scaffolding), aimed at a macOS SQLite corruption issue with the current file-mount transport.
- Hardened the pre-commit hook: strips zero-width/invisible characters and em dashes from staged files, and handles non-ASCII filenames correctly.
